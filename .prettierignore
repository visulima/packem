.gitkeep
.env*
*.ico
*.lock
dist
CHANGELOG.md
coverage
node_modules
.eslintcache

**/tsconfig.json
**/jsconfig.json

pnpm-lock.yaml
# pnpm rewrites this file itself (catalog bumps, `pnpm update`), with its own
# two-space list indentation. Formatting it here only makes the next install drift.
pnpm-workspace.yaml

# Generated by rollup-plugin-license during the build. Formatting them here makes the
# next build dirty the tree again.
**/LICENSE.md

# Source phase imports (`import source m from "./m.wasm"`) are a stage 3 proposal that
# prettier's parser cannot yet handle. The wasm example demonstrates the syntax on
# purpose — packem rewrites it before any transformer sees it.
examples/wasm-esm/src/source-phase.js
