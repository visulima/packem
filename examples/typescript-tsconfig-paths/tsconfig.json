{
    "$schema": "https://json.schemastore.org/tsconfig",
    "include": ["src/**/*", "*.d.ts"],
    "compilerOptions": {
        "baseUrl": "src",
        "paths": {
            "components:*": ["components/*.ts"]
        }
    }
}
