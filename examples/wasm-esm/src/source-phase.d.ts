// Hand-written because the implementation is JavaScript (see source-phase.js for why).
export declare const countCalls: () => {
    calls: () => number;
    logSum: (a: number, b: number) => void;
};
