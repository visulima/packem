name: "Dependency review"
on: # yamllint disable-line rule:truthy
    pull_request:
        branches: ["main", "alpha"]
# Only the newest review of a pull request is meaningful.
concurrency:
    group: "${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}"
    cancel-in-progress: true

permissions: {}
jobs:
    dependency-review:
        permissions:
            contents: "read"
            pull-requests: "write"
        uses: "anolilab/workflows/.github/workflows/dependency-review.yml@1641a17797e6e1dc3eaf9b462c3c0dae91ab071d" # v20.2.12
