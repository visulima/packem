---
title: Source Maps
description: Configure source map generation for debugging
---

import { Callout } from 'fumadocs-ui/components/callout'

# Source Maps

Source maps provide a way to map compiled code back to the original source code, enabling debugging of the original code in browsers and other tools. Packem supports various source map formats and configurations.

## Basic Configuration

### Enable Source Maps

The simplest way to enable source maps for every output is the top-level `sourcemap` boolean:

```typescript
import { defineConfig } from '@visulima/packem/config'
import transformer from '@visulima/packem/transformer/esbuild'

export default defineConfig({
  transformer,
  sourcemap: true,
})
```

For per-output control, set `sourcemap` on the Rollup output instead:

```typescript
export default defineConfig({
  rollup: {
    output: {
      sourcemap: true
    }
  }
})
```

### Source Map Types

```typescript
export default defineConfig({
  rollup: {
    output: [
      {
        format: 'esm',
        file: 'dist/index.mjs',
        sourcemap: true // External source map
      },
      {
        format: 'cjs',
        file: 'dist/index.cjs',
        sourcemap: 'inline' // Inline source map
      }
    ]
  }
})
```

## Source Map Formats

### External Source Maps

```typescript
export default defineConfig({
  rollup: {
    output: {
      sourcemap: true, // Creates .map files
      sourcemapFile: 'dist/index.js.map'
    }
  }
})
```

### Inline Source Maps

```typescript
export default defineConfig({
  rollup: {
    output: {
      sourcemap: 'inline' // Embedded in the bundle
    }
  }
})
```

### Hidden Source Maps

```typescript
export default defineConfig({
  rollup: {
    output: {
      sourcemap: 'hidden' // Generated but not referenced
    }
  }
})
```

## Environment-Specific Configuration

### Development vs Production

```typescript
export default defineConfig({
  rollup: {
    output: {
      sourcemap: process.env.NODE_ENV === 'development' ? true : false
    }
  }
})
```

### Conditional Source Maps

```typescript
export default defineConfig({
  rollup: {
    output: {
      sourcemap: process.env.GENERATE_SOURCEMAP !== 'false'
    }
  }
})
```

## Transformer-Specific Source Maps

Transformer source map options live under the matching transformer key inside `rollup`.

### esbuild Source Maps

```typescript
import { defineConfig } from '@visulima/packem/config'
import transformer from '@visulima/packem/transformer/esbuild'

export default defineConfig({
  transformer,
  sourcemap: true,
  rollup: {
    esbuild: {
      sourceMap: true,
    },
  },
})
```

### SWC Source Maps

```typescript
import { defineConfig } from '@visulima/packem/config'
import transformer from '@visulima/packem/transformer/swc'

export default defineConfig({
  transformer,
  sourcemap: true,
  rollup: {
    swc: {
      sourceMaps: true,
    },
  },
})
```

## CSS Source Maps

CSS source maps are configured via the `sourceMap` option of the CSS processor, which lives under `rollup.css`. It accepts `true`, `false`, or `'inline'`.

```typescript
import { defineConfig } from '@visulima/packem/config'
import transformer from '@visulima/packem/transformer/esbuild'

export default defineConfig({
  transformer,
  rollup: {
    css: {
      sourceMap: true,
    },
  },
})
```

For inline CSS source maps:

```typescript
export default defineConfig({
  rollup: {
    css: {
      sourceMap: 'inline',
    },
  },
})
```

## Advanced Configuration

### Source Map Paths

```typescript
export default defineConfig({
  rollup: {
    output: {
      sourcemap: true,
      sourcemapPathTransform: (relativeSourcePath, sourcemapPath) => {
        // Transform source paths in source maps
        return relativeSourcePath.replace('../src/', 'src/')
      }
    }
  }
})
```

### Source Content Inclusion

```typescript
export default defineConfig({
  rollup: {
    output: {
      sourcemap: true,
      sourcemapExcludeSources: false, // Include source content
      sourcemapFile: 'dist/index.js.map'
    }
  }
})
```

### Base URL Configuration

```typescript
export default defineConfig({
  rollup: {
    output: {
      sourcemap: true,
      sourcemapBaseUrl: 'https://cdn.example.com/src/'
    }
  }
})
```

## Framework Integration

### React Source Maps

```typescript
export default defineConfig({
  rollup: {
    plugins: [
      react({
        // Preserve JSX source locations
        development: true
      })
    ],
    
    output: {
      sourcemap: true
    }
  }
})
```

### Vue Source Maps

```typescript
export default defineConfig({
  rollup: {
    plugins: [
      vue({
        // Include template source maps
        sourceMap: true
      })
    ],
    
    output: {
      sourcemap: true
    }
  }
})
```

## Debugging Configuration

### Browser DevTools

```typescript
export default defineConfig({
  rollup: {
    output: {
      sourcemap: true,
      // Optimize for browser debugging
      sourcemapExcludeSources: false
    }
  }
})
```

### Node.js Debugging

```typescript
export default defineConfig({
  rollup: {
    output: {
      format: 'cjs',
      sourcemap: true,
      // Enable source map support
      banner: 'require("source-map-support").install();'
    }
  }
})
```

### VS Code Integration

```json
// .vscode/launch.json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Bundle",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/dist/index.js",
      "sourceMaps": true,
      "outFiles": ["${workspaceFolder}/dist/**/*.js"]
    }
  ]
}
```

## Performance Considerations

### Source Map Size

```typescript
export default defineConfig({
  rollup: {
    output: {
      // Smaller source maps
      sourcemap: process.env.NODE_ENV === 'development' ? true : 'hidden'
    }
  }
})
```

### Build Performance

```typescript
export default defineConfig({
  rollup: {
    output: {
      sourcemap: true,
      // Exclude sources content for faster builds
      sourcemapExcludeSources: true
    }
  }
})
```

### Network Performance

```typescript
export default defineConfig({
  rollup: {
    output: {
      // Use hidden source maps for production
      sourcemap: process.env.NODE_ENV === 'production' ? 'hidden' : true
    }
  }
})
```

## Source Map Validation

### Verification

```typescript
export default defineConfig({
  hooks: {
    'build:done': async () => {
      // Validate source maps
      const { SourceMapConsumer } = await import('source-map')
      const fs = await import('fs/promises')
      
      const mapContent = await fs.readFile('dist/index.js.map', 'utf-8')
      const consumer = await new SourceMapConsumer(JSON.parse(mapContent))
      
      // Test source map accuracy
      const originalPosition = consumer.originalPositionFor({
        line: 1,
        column: 0
      })
      
      console.log('Source map validation:', originalPosition)
      consumer.destroy()
    }
  }
})
```

### Testing Tools

```typescript
export default defineConfig({
  rollup: {
    plugins: [
      {
        name: 'sourcemap-test',
        generateBundle(options, bundle) {
          // Test source map generation
          Object.keys(bundle).forEach(fileName => {
            if (fileName.endsWith('.map')) {
              const map = JSON.parse(bundle[fileName].source)
              console.log(`Source map for ${fileName}:`, {
                version: map.version,
                sources: map.sources.length,
                mappings: map.mappings.length
              })
            }
          })
        }
      }
    ]
  }
})
```

## Troubleshooting

### Missing Source Maps

<Callout type="warning">
If source maps aren't working, ensure all transformers have source map support enabled.
</Callout>

```typescript
import { defineConfig } from '@visulima/packem/config'
import transformer from '@visulima/packem/transformer/esbuild'

export default defineConfig({
  transformer,
  // Enable source maps at all levels
  sourcemap: true,
  rollup: {
    output: {
      sourcemap: true,
    },
    esbuild: {
      sourceMap: true,
    },
    css: {
      sourceMap: true,
    },
  },
})
```

### Incorrect Source Paths

```typescript
export default defineConfig({
  rollup: {
    output: {
      sourcemap: true,
      sourcemapPathTransform: (relativeSourcePath) => {
        // Fix source paths
        return relativeSourcePath.replace(/\\/g, '/')
      }
    }
  }
})
```

### Large Source Maps

```typescript
export default defineConfig({
  rollup: {
    output: {
      sourcemap: true,
      // Exclude node_modules from source maps
      sourcemapIgnoreList: (relativeSourcePath) => {
        return relativeSourcePath.includes('node_modules')
      }
    }
  }
})
```

## Security Considerations

### Production Source Maps

```typescript
export default defineConfig({
  rollup: {
    output: {
      // Don't expose source maps in production
      sourcemap: process.env.NODE_ENV === 'development' ? true : false
    }
  }
})
```

### Source Content Protection

```typescript
export default defineConfig({
  rollup: {
    output: {
      sourcemap: true,
      // Don't include source content in production
      sourcemapExcludeSources: process.env.NODE_ENV === 'production'
    }
  }
})
```

---

## Related Options

- [Target](/docs/options/target) - Compilation targets
- [Minify](/docs/options/minify) - Code minification
- [Watch](/docs/options/watch) - Development workflow
- [Debug](/docs/options/debug) - Debugging options