---
title: rootDir
description: Project root directory for resolving all relative paths
---

# rootDir

The root directory of your project. Used as the base for resolving all relative paths.

**Type:** `string`  
**Default:** Project root directory

## Example

```typescript
export default defineConfig({
  rootDir: __dirname
})
```