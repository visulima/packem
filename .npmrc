# npm v8.5+: npm ERR! notarget No matching version found for...
workspaces-update=false

# CLI behavior flags
fund=false
audit=false
progress=false

# Allow workspace cycles for self-bootstrapping packages (packem builds itself)
disallow-workspace-cycles=false
