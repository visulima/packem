export const legacyHelper = (): number => 1;

export const renamedHelper = (): number => 2;

export type LegacyOptions = { a: number };

/** Doc written on the declaration itself. */
export type DocumentedOptions = { b: number };
