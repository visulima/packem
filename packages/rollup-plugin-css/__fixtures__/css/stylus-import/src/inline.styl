// Placeholder for unit tests that render stylus content inline.
// Stylus stats this path when linenos is enabled and the stylus
// parser cache is keyed on filename, so a real file must exist.
.placeholder
  color inherit
