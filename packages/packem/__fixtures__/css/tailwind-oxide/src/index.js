import "./styles.css";
import { Button, Card } from "./components/index.js";

console.log("Tailwind Oxide test loaded");

// This will ensure all component classes are included in the build
export { Button, Card };
