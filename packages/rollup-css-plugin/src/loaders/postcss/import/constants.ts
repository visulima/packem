/**
 * Modified copy of https://github.com/csstools/postcss-plugins/blob/main/plugin-packs/postcss-bundler/src/postcss-import/lib/names.ts
 *
 * MIT No Attribution (MIT-0)
 * Copyright © CSSTools Contributors
 */

export const IS_CHARSET_REGEX: RegExp = /^charset$/i;

export const IS_IMPORT_REGEX: RegExp = /^import$/i;

export const IS_URL_REGEX: RegExp = /^url$/i;

export const IS_LAYER_REGEX: RegExp = /^layer$/i;

export const IS_SUPPORTS_REGEX: RegExp = /^supports$/i;

export const IS_SCOPE_REGEX: RegExp = /^scope$/i;
